#!/bin/bash

mkdir travis_release

cp -r * travis_release

rm -f   travis_release/.travis.yml
rm -rf  travis_release/bin
rm -rf  travis_release/travis_release

cd travis_release

mkdir oyst
rsync -av * oyst --exclude='oyst'
zip -r oyst.zip oyst
tar -cvzf oyst.tar.gz oyst
